# Zeabur 快速部署指南 ⚡

只需 **5 分钟**，将你的博客部署到云端！

## 🎯 部署步骤

### 1️⃣ 注册 Zeabur

访问 [zeabur.com](https://zeabur.com) → 使用 GitHub 登录

### 2️⃣ 创建项目

1. 点击 **"创建项目"** (Create Project)
2. 输入项目名：`my-blog`
3. 选择区域：**Hong Kong** 或 **Singapore**

### 3️⃣ 部署后端

1. 点击 **"添加服务"** (Add Service)
2. 选择 **"Git"**
3. 选择你的仓库：`Claude-Code`
4. 填写配置：
   - **服务名称**: `blog-server`
   - **分支**: `main` 或你的分支名
   - **根目录**: `server`
5. 点击 **"部署"** (Deploy)

⏱️ 等待 1-2 分钟构建完成

### 4️⃣ 获取后端地址

部署成功后：
1. 进入 `blog-server` 服务
2. 点击 **"网络"** (Networking) 或 **"域名"**
3. 复制域名，例如：`https://blog-server-abc123.zeabur.app`

### 5️⃣ 部署前端

1. 再次点击 **"添加服务"**
2. 选择 **"Git"** → 同一个仓库
3. 填写配置：
   - **服务名称**: `blog-client`
   - **分支**: `main` 或你的分支名
   - **根目录**: `client`
4. **重要！** 在部署前，点击 **"变量"** (Variables)
5. 添加环境变量：
   ```
   名称: VITE_API_URL
   值: https://blog-server-abc123.zeabur.app
   ```
   （填入你在步骤4复制的后端域名）
6. 点击 **"部署"**

⏱️ 等待 2-3 分钟构建完成

### 6️⃣ 访问你的博客

1. 进入 `blog-client` 服务
2. 点击生成的域名，例如：`https://blog-client-xyz789.zeabur.app`
3. 🎉 恭喜！你的博客已经上线了！

---

## 📝 配置数据持久化（重要！）

为了避免重新部署时数据丢失：

### 后端添加存储卷

1. 进入 `blog-server` 服务
2. 点击 **"存储"** (Volumes) 或 **"持久化存储"**
3. 点击 **"添加 Volume"**
4. 填写：
   - **挂载路径**: `/app/src/data`
5. 保存

✅ 现在你的文章数据会永久保存！

---

## 🌐 绑定自定义域名（可选）

### 1. 添加域名

1. 进入 `blog-client` 服务
2. 点击 **"网络"** → **"添加域名"**
3. 输入你的域名：`blog.yourdomain.com`

### 2. 配置 DNS

在你的域名提供商（阿里云、腾讯云等）：

1. 添加 **CNAME 记录**：
   ```
   类型: CNAME
   主机记录: blog
   记录值: [Zeabur 提供的 CNAME]
   ```

2. 等待 DNS 生效（通常 5-10 分钟）

### 3. 自动 HTTPS

Zeabur 会自动为你的域名配置 SSL 证书！

---

## ✅ 部署检查清单

部署完成后，测试以下功能：

- [ ] 前端页面能正常打开
- [ ] 点击 "新建文章" 能创建文章
- [ ] 能够编辑文章标题和内容
- [ ] 文章能够自动保存
- [ ] 能够删除文章
- [ ] 刷新页面后文章还在（数据持久化）

---

## 🔧 环境变量配置参考

### 前端 (blog-client)

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `VITE_API_URL` | `https://blog-server-xxx.zeabur.app` | 后端 API 地址 |

### 后端 (blog-server)

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `PORT` | `3001` | 端口（可选，Zeabur 会自动配置） |
| `NODE_ENV` | `production` | 运行环境 |

---

## 🐛 常见问题

### Q1: 前端显示 "网络错误" 或无法加载文章

**原因**: 前端无法连接到后端

**解决**:
1. 检查后端服务是否正常运行
2. 检查前端环境变量 `VITE_API_URL` 是否正确
3. 确保后端域名以 `https://` 开头
4. 重新部署前端

### Q2: 重新部署后文章丢失

**原因**: 没有配置数据持久化

**解决**:
1. 为后端服务添加 Volume
2. 挂载路径设置为 `/app/src/data`

### Q3: 部署失败

**常见原因**:
- 根目录路径填写错误（应该是 `server` 或 `client`）
- 分支名称错误
- Node.js 版本不兼容

**解决**:
1. 查看部署日志
2. 检查配置是否正确
3. 确保 GitHub 代码是最新的

### Q4: CORS 错误

**现象**: 浏览器控制台显示 CORS 相关错误

**解决**:
代码已配置支持 Zeabur 域名，如果还有问题：
1. 确认后端服务正常运行
2. 检查前端请求的 URL 是否正确
3. 查看后端日志

---

## 📊 监控和维护

### 查看日志

1. 进入对应服务（前端或后端）
2. 点击 **"日志"** (Logs)
3. 实时查看运行日志

### 重新部署

1. 修改代码后 push 到 GitHub
2. Zeabur 会自动检测并重新部署
3. 或手动点击 **"重新部署"** 按钮

### 资源使用

在服务页面可以查看：
- CPU 使用率
- 内存使用率
- 网络流量
- 请求次数

---

## 💰 费用说明

Zeabur 免费套餐包括：
- ✅ 每月 $5 美元等值额度
- ✅ 免费域名 (`*.zeabur.app`)
- ✅ 免费 SSL 证书
- ✅ 无限请求次数

个人博客通常不会超出免费额度！

---

## 🎉 部署成功！

你的博客现在已经：
- ✅ 运行在云端
- ✅ 支持 HTTPS
- ✅ 全球可访问
- ✅ 自动部署
- ✅ 数据持久化

开始写作吧！✨

---

## 📞 需要帮助？

遇到问题可以：
1. 查看 [Zeabur 官方文档](https://zeabur.com/docs)
2. 查看项目的 `ZEABUR-DEPLOYMENT.md` 详细指南
3. 在 GitHub Issues 提问
4. 告诉我具体的错误信息

祝你使用愉快！🚀
